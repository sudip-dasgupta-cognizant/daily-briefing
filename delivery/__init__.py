"""Delivery sub-package for the Daily Briefing application.

Contains the email delivery module that sends the generated MP3 audio file
and the script text to the configured recipient via Gmail SMTP using a
Google App Password (no OAuth flow required).
"""
