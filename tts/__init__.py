"""TTS (Text-to-Speech) sub-package for the Daily Briefing application.

Wraps the ``edge-tts`` library to convert a briefing script string into an
MP3 audio file stored in the ``output/`` directory.  edge-tts is a free,
offline-capable TTS engine that uses Microsoft Edge's neural voices.
"""
