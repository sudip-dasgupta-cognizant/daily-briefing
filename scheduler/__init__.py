"""Scheduler sub-package for the Daily Briefing application.

Uses APScheduler to run the full briefing pipeline automatically at a
configured time each day, so the briefing arrives in the recipient's inbox
without any manual intervention.
"""
