"""Fetchers sub-package for the Daily Briefing application.

Exposes thin wrappers around external data sources:
  - ``weather``: current conditions from wttr.in (no API key required)
  - ``news``: top headlines from the GNews API
"""
